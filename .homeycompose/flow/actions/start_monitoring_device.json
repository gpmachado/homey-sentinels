{
  "title": { "en": "Start monitoring device" },
  "titleFormatted": { "en": "Start monitoring device [[device]], name [[name]]" },
  "hint": { "en": "For an on/off-only device (a pump, a device with no reliable standby signal) — creates the monitor on first run if it doesn't exist yet, then starts it, in one step. Meant to be driven by a native \"Power becomes greater than X\" trigger (or any other Flow logic) instead of this app's own threshold engine. The resulting monitor never decides active/standby on its own — only this card and \"Stop monitoring device\" do." },
  "args": [
    { "name": "device", "type": "autocomplete", "title": { "en": "Device" } },
    { "name": "name", "type": "text", "required": false, "title": { "en": "Monitor name (optional)" } }
  ],
  "tokens": [
    { "name": "device", "type": "string", "title": { "en": "Device" } },
    { "name": "monitor", "type": "string", "title": { "en": "Monitor" } },
    { "name": "power", "type": "number", "title": { "en": "Power (W)" } },
    { "name": "timestamp", "type": "string", "title": { "en": "Timestamp" } },
    { "name": "message", "type": "string", "title": { "en": "Message" } }
  ]
}
